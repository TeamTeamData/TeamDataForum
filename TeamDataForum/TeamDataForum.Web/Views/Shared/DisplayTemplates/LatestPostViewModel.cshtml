@using TeamDataForum.Web.Models.ViewModels.Posts
@model LatestPostViewModel

@if (Model != null)
{
    <div>@Html.ActionLink(Model.Title, "Threads", new { Id = Model.ThreadId })</div>
    <div>@Model.Author</div>
    <div>@Model.Date</div>
}